class SENDER {
	private root = '/sender'

	HOME = this.root

	PROFILE = `${this.root}/profile`
	CREATE = `${this.root}/create`
	HISTORY = `${this.root}/history`
}
export const SENDER_PAGES = new SENDER()
