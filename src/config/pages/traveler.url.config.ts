class TRAVELER {
	private root = '/fellow-traveler'

	HOME = this.root

	PROFILE = `${this.root}/profile`
	CREATE = `${this.root}/create`
	HISTORY = `${this.root}/history`
}
export const TRAVELER_PAGES = new TRAVELER()
